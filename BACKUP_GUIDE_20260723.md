# 2026년 7월 23일 백업 및 복원 가이드

오늘 날짜(2026-07-23)를 기준으로 프로젝트 상태를 나중에 복원하거나 되돌릴 수 있도록 조치하였습니다.

## 1. 백업 기준 정보
* **기준 날짜**: 2026년 7월 23일 (목요일)
* **기준 커밋 해시**: `9f3e0a8fcf03644637e1102d0b580ec5edc35214`
* **백업용 Git 태그**: `backup-2026-07-23`
* **백업용 Git 브랜치**: `backup-branch-2026-07-23`

---

## 2. 복원 방법 (되돌리는 방법)

상황에 맞게 아래 방법 중 하나를 선택하여 진행해 주세요. 터미널(Git Bash, PowerShell 등)을 열고 프로젝트 폴더(`C:\ANCODING\agy`)에서 명령어를 실행하시면 됩니다.

### 방법 A: 오늘 시점으로 코드를 임시로 확인하거나 조회하고 싶을 때 (안전한 조회용)
이 방법은 기존 코드를 훼손하지 않고 오늘 시점의 코드 상태를 임시로 탐색할 때 사용합니다.
```bash
# 오늘 날짜 태그로 체크아웃
git checkout backup-2026-07-23
```
*다시 최신 상태로 돌아오려면:*
```bash
git checkout main
```

### 방법 B: 오늘 시점부터 새로운 작업을 이어서 하고 싶을 때 (독립적인 새 작업용)
오늘 백업해 둔 시점의 브랜치로 이동하여 작업을 새로 이어나갈 수 있습니다.
```bash
# 오늘 백업용 브랜치로 전환
git checkout backup-branch-2026-07-23
```

### 방법 C: 현재 작업을 모두 버리고 오늘 시점으로 완전히 되돌리기 (강제 리셋)
**⚠️ 주의: 현재 작업 중이던 저장하지 않은 모든 변경사항이 사라집니다.**
현재 `main` 브랜치의 상태를 오늘 백업 시점으로 완전히 강제 리셋합니다.
```bash
# 1. main 브랜치로 이동
git checkout main

# 2. main 브랜치를 오늘 백업 태그 시점으로 강제 리셋
git reset --hard backup-2026-07-23
```

### 방법 D: 원격 저장소(GitHub 등)에 백업 정보 동기화해 두기 (서버에 백업 저장)
만약 로컬 컴퓨터 외에 원격 GitHub 저장소에도 백업 태그와 브랜치를 업로드해두어 컴퓨터 포맷 등에 대비하려면 아래 명령어를 사용하세요.
```bash
# 태그 원격 전송
git push origin backup-2026-07-23

# 브랜치 원격 전송
git push origin backup-branch-2026-07-23
```
